# Kiln tooling DX — make the engine easy for the human and the agent

> **For Hermes:** Execute task-by-task. Do not `nix flake check` or `nix build .#petabyte-madness` on the laptop unless the user asks. Do not push. Do not invent a second CLI.

**Goal:** One short contract both humans and agents can follow, plus a cheap `./dev` path that does not boot a ROM or hash the whole flake.

**Architecture:** The loop already exists (`./dev` in Kiln, a sibling `./dev` in PetaByte Madness, four skills under `.claude/skills/`, gates under `nix/checks/`). The failure is discoverability: Hermes loads `CLAUDE.md` (90k) and does not load `.claude/skills/`; agents default to `nix flake check` and `./dev drive`; the game flake NAR-hashes the engine. Add a one-screen `AGENTS.md` in each repo and a `./dev cheap` that runs the fast native checks. Do not wrap Nix in a new tool.

**Tech stack:** bash `./dev`, Nix checks (`blender-tests`, `kiln-logic`, `kiln-gui`), `AGENTS.md` (Hermes/Codex auto-load).

**Repos:** `/home/asher/Documents/M64` (Kiln, public origin, do not push unless asked). `/home/asher/Documents/PetaByte-Madness` (no remotes).

---

## Current context / assumptions

This session's actual friction, not a wishlist:

| What happened | Why |
|---|---|
| `skill_view(n64-verify)` failed | Skills live in `.claude/skills/`, not `~/.hermes/skills/` |
| Agents planned `./dev drive` for one screen | Jump ROMs exist (`.#pm-jump-*`, `.#forge-cam`); `n64-verify` says prefer them; nothing in a 1-screen agent brief says so |
| `nix flake check` is the documented gate | User games on this laptop; full check builds every demo ROM |
| PBM `flake.lock` NAR-hashes `path:/home/asher/Documents/M64` | PBM `./dev` already passes `--override-input kiln`; agents that run raw `nix build` still die |
| Two `./dev` scripts | Correct: Kiln's `cd`s to its own tree (`M64/dev:8-11`). PBM's header says so (`PetaByte-Madness/dev:6-12`). Do not merge them |
| Claude Opus 429 mid-armature | Out of scope for engine DX. Leave `tools/blender/demonrig.py` untracked |

Keep: PBM `./dev screens`, `./dev ab`, USAGE-BEGIN/END so help cannot drift. Kiln `./dev` auto-enters `nix develop`. Host rasteriser gates (`kiln-gui`). Jump ROMs.

---

## Findings (review)

### Sound — keep

- PBM `./dev` USAGE markers (`PetaByte-Madness/dev:14-26`, printed at `:74-77`) — copy that pattern if Kiln help grows.
- PBM `--override-input kiln path:$KILN_SRC` (`PetaByte-Madness/dev:28-45`) — the sibling-checkout contract.
- Verification ladder in `.claude/skills/n64-verify/SKILL.md`: gates → jump ROM → pixel stats → overlay. Do not rewrite it.
- `nix/checks/blender-tests.nix` + `kiln-logic.nix` + `kiln-gui.nix` are the fast loops (`flake.nix:1206`, `:1286`, `:1226`).

### Broken for agents

- **No `AGENTS.md` in either repo.** Hermes injects it; Codex looks for it. `CLAUDE.md` is 1446 lines. Agents do not extract the 12 lines they need.
- **Kiln `./dev help` omits poser from the first block** (`M64/dev:18-35` vs arms at `:290-301`). `mapmaker`/`poser` only appear under Examples.
- **No cheap gate command.** `./dev check` is `nix flake check --print-build-logs` (`M64/dev:280-281`) — every `rom-*` check in `flake.nix:1036+`.
- **Skills are Claude-Code-shaped.** An agent in this session could not `skill_view` them.

### YAGNI — do not do

- A unified monorepo `./dev` that `cd`s between Kiln and PBM.
- Copying four skills into `~/.hermes/skills/` (user-global, will rot).
- A new MCP / blender-mcp requirement for day-to-day C.
- Auto-commit hooks, dashboards, or a TUI.

---

## Proposed approach

1. `AGENTS.md` at each repo root: 40–60 lines, imperative facts, pointers to `./dev` and `.claude/skills/`.
2. `./dev cheap` in Kiln (and PBM): `nix build` **only** the fast checks.
3. Kiln `usage()` lists poser/mapmaker next to shot/drive.
4. PBM `AGENTS.md` states: always `./dev`, never raw `nix build`, jump via `./dev screens`.

---

## Step-by-step tasks

### Task 1: Kiln `AGENTS.md`

**Objective:** Any agent opening `/home/asher/Documents/M64` gets the contract without reading 90k of `CLAUDE.md`.

**Files:**
- Create: `/home/asher/Documents/M64/AGENTS.md`

**Step 1: Write this file exactly** (edit only if a command below is wrong when you run it):

```markdown
# AGENTS.md — Kiln

You are in the **engine** repo (libkiln + Nix + `./dev`). Games are siblings, not submodules: `~/Documents/PetaByte-Madness`, `~/Documents/ganja-goblin`.

## Do this

1. `./dev help` — the CLI. Build/run/shot/drive/check/doctor/forge/poser live here.
2. Prefer a **jump ROM** over `./dev drive` for one screen or mode. Drive's uinput→SDL→ares chain is fragile; `./dev shot` has no input path. See `.claude/skills/n64-verify/SKILL.md`.
3. Visual/geometry/animation work: **read** `.claude/skills/n64-verify/SKILL.md`, `n64-modeling/SKILL.md`, `n64-animation/SKILL.md`, `n64-forge/SKILL.md` with the file tool. They are not Hermes-installed skills.
4. Fast gates (no ROM): `./dev cheap`
5. Full pre-push: `./dev check` — **not on the laptop while the user is playing unless they ask.**

## Do not

- `nix flake check` or a full ROM build unless asked.
- Push to `origin` (`github.com/ALH477/kiln.git`) unless asked.
- Reintroduce the name `M64` as an engine identifier (`nix/checks/kiln-names.nix`).
- Trust emulator audio. Validate audio on hardware.
- Type a dimension into C that a generator already emits. See n64-modeling.

## Layout

- `engine/src/kiln/` — one `MODULES` list in `engine/modules.mk`
- `nix/` — toolchain, libdragon, rom, faust gates, host math
- `plat/host/` — software rasteriser for sandbox-safe 2D/3D checks
- `./dev` — this tree only (`cd`s here). A game has its own `./dev`.
```

**Step 2: Verify**

```bash
test -f /home/asher/Documents/M64/AGENTS.md && wc -l /home/asher/Documents/M64/AGENTS.md
```

Expected: file exists, **< 80 lines**.

**Step 3: Commit** (Kiln repo)

```bash
cd /home/asher/Documents/M64
git add AGENTS.md
git commit -m "AGENTS.md: the one screen an agent should read first"
```

---

### Task 2: `./dev cheap` on Kiln

**Objective:** Fast native checks without building `hello.z64` and friends.

**Files:**
- Modify: `/home/asher/Documents/M64/dev` — `usage()` heredoc and `case`

**Step 1: Add to the `usage()` cat (after `check` line, around line 35)**

```
  cheap                    blender-tests + kiln-logic + kiln-gui (no ROMs)
```

**Step 2: Add a `cheap)` arm before `check)` (~line 280)**

```bash
  cheap)
    # The fast loops. Not `flake check`: that builds every demo ROM.
    # Host/gui/logic/python only — safe on the laptop.
    nix build --print-out-paths \
      .#checks.x86_64-linux.blender-tests \
      .#checks.x86_64-linux.kiln-logic \
      .#checks.x86_64-linux.kiln-gui
    ;;
```

If `uname -m` is not x86_64, use `nix eval --raw --impure --expr builtins.currentSystem` once and substitute. Do not hardcode a second time; one `SYS=$(nix eval --raw --impure --expr builtins.currentSystem)` at the top of the arm is enough.

**Step 3: Verify help**

```bash
./dev help | grep cheap
```

Expected: a line containing `cheap` and `no ROM`.

**Step 4: Run cheap** (this *is* nix, but three small checks, not the ROM farm). If the user said no nix at all this session, skip the run and still commit the arm; report that.

```bash
./dev cheap
```

Expected: three `/nix/store/...-check-*` paths, exit 0. On first run it may compile host stubs — still not a MIPS ROM.

**Step 5: Commit**

```bash
git add dev
git commit -m "./dev cheap: the gates that do not build a ROM"
```

---

### Task 3: Kiln help lists poser and mapmaker in the main block

**Objective:** `./dev help` matches the case arms.

**Files:** `/home/asher/Documents/M64/dev` `usage()` heredoc (~lines 18–50)

Add these lines in the main list, not only under Examples:

```
  mapmaker                 three.js .map editor on :8000
  map-validate <file.map>  round-trip through quake_map.py
  poser / poser-stage / poser-verify
                           three.js animation editor on :8001
```

**Verify:**

```bash
./dev help | grep -E 'poser|mapmaker'
```

Expected: both names in the first block, not only after `Examples:`.

Commit with Task 2 if still uncommitted; else:

```bash
git commit -m "./dev help: poser and mapmaker are first-class commands"
```

---

### Task 4: PetaByte `AGENTS.md`

**Objective:** Agents in the game repo stop raw-`nix build`ing and stop using Kiln's `./dev`.

**Files:**
- Create: `/home/asher/Documents/PetaByte-Madness/AGENTS.md`

```markdown
# AGENTS.md — PetaByte Madness

This is the **game**. The engine is `~/Documents/M64` (Kiln). Not a submodule.

## Do this

1. Use **this** `./dev`, not Kiln's. Kiln's `cd`s to M64 and builds *its* flake.
2. `./dev help` — build/run/shot/drive/rec/ab/check/screens.
3. `./dev` already passes `--override-input kiln path:$KILN_SRC` so a dirty engine tree does not NAR-hash-mismatch. Raw `nix build .#petabyte-madness` will.
4. To look at one screen: `./dev screens` then `nix build` a `pm-jump-*` / `pm-veil-*` name, then `./dev shot <that> out.png`. Do not `./dev drive` to reach it.
5. Specs: `docs/VEIL_DESIGN.md` before `src/pm_veil.*`. `docs/ASSET_PIPELINE.md` before models.
6. Fast gates: `./dev cheap` (once Task 5 lands). No full ROM on the laptop unless asked.

## Rooms (do not unify them)

- PLAY walks `assets/pm_lab.map`.
- Intro LAB walks `dank_lab` + `src/pm_lab.c` brushes — not that map.
- The island is flyover/attract, not a clip world.

## Do not

- Push (this repo has no remotes; keep it that way unless asked).
- `git add` untracked `tools/blender/demonrig.py` until `--verify` on all four demons is green *and* `flake.nix` `pmDemonModel` has `animated = true`.
- Re-do the CI4/TLUT bake; it exists (`mkVeilTexture`).
```

**Verify:** `wc -l` < 50. Commit **in the PBM repo**:

```bash
cd /home/asher/Documents/PetaByte-Madness
git add AGENTS.md
git commit -m "AGENTS.md: this ./dev, not Kiln's, and do not unify the rooms"
```

---

### Task 5: `./dev cheap` on PetaByte Madness

**Objective:** Game-side fast checks, no `.z64`.

**Files:** `/home/asher/Documents/PetaByte-Madness/dev`

Add to USAGE-BEGIN/END:

```
#   ./dev cheap                     blender-tests + pm-rigs + pm-cine + pm-gen-headers
```

Add arm (keep `"${NIXARGS[@]}"`):

```bash
  cheap)
    nix build --print-out-paths "${NIXARGS[@]}" \
      .#checks.x86_64-linux.blender-tests \
      .#checks.x86_64-linux.pm-rigs \
      .#checks.x86_64-linux.pm-cine \
      .#checks.x86_64-linux.pm-gen-headers \
      .#checks.x86_64-linux.pm-demon-textures
    ;;
```

**Verify:**

```bash
./dev help | grep cheap
```

Expected: the new line.

Run `./dev cheap` only if the user allows nix. Commit.

```bash
git commit -m "./dev cheap: the game gates that do not build a ROM"
```

---

### Task 6 (optional, same PR if still in Kiln): point CLAUDE.md at AGENTS.md

**Files:** `/home/asher/Documents/M64/CLAUDE.md` — **one line** under Commands, not a rewrite.

After the opening "What this is" paragraph, add:

```
Agents: read `AGENTS.md` first. This file is the long form.
```

Do not duplicate the command table. Commit only if the diff is ≤ 5 lines.

---

## Tests / validation

| Task | Command | Expected |
|---|---|---|
| 1 | `wc -l AGENTS.md` in M64 | < 80, file tracked |
| 2 | `./dev help \| grep cheap` | one line |
| 2 | `./dev cheap` | three store paths, exit 0 |
| 3 | `./dev help \| grep poser` | poser in help body |
| 4 | `test -f ~/Documents/PetaByte-Madness/AGENTS.md` | exists |
| 5 | `./dev help \| grep cheap` in PBM | one line |

No ROM. No screenshot. TDD here is "command missing → add arm → command present".

---

## Risks, tradeoffs, open questions

- `./dev cheap` still hits Nix. It is smaller than `flake check`, not zero. If even that is too heavy, document `python3 tools/blender/test_*.py` as the zero-nix subset and keep `cheap` as the Nix-shaped one.
- `checks.x86_64-linux.*` is wrong on aarch64 — use `currentSystem` in the arm.
- `AGENTS.md` will rot if someone adds a `./dev` command and forgets it. PBM already solved that with USAGE-BEGIN; Kiln help is a hand-kept heredoc — Task 3 is the last help edit unless you switch Kiln to the same marker pattern (optional follow-up, one extra commit).
- Installing skills into Hermes globally will drift from `.claude/skills/`. Pointers in `AGENTS.md` are the DRY fix.

---

## Execution handoff

Plan complete. Execute Tasks 1→5 in order, commit per task, two repos. Do not push. Do not touch `demonrig.py` or scene geometry in this workstream.
