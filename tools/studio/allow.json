{
  "//": "The files Kiln Studio's editors may read and save (studio/fsapi.py). One glob per entry, matched segment by segment: a * never crosses a /. `editor` names the panel that opens it; `validator` runs automatically after a save; `except` names files the glob matches that stay read-only (the poser's index is generated by anim_io.py).",
  "patterns": [
    { "glob": "assets/*.map", "editor": "map", "validator": "map-validate" },
    { "glob": "assets/*.shot.json", "validator": "camlint" },
    { "glob": "assets/*/*.shot.json", "validator": "camlint" },
    { "glob": "assets/rig/*.json" },
    { "glob": "tools/poser/data/*.*.json", "except": ["*.index.json"], "editor": "pose" },
    { "glob": "tools/schema/level_vocab.json" }
  ]
}
